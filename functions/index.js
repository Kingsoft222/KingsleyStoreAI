import './app.js';

/**
 * Kingsley Store AI Entry Point
 * This file initializes the boutique application.
 */
console.log("Kingsley Store AI: Entry point initialized.");

// Optional: Global error handling for the app
window.onerror = function(message, source, lineno, colno, error) {
    console.error("Global App Error: ", message);
};